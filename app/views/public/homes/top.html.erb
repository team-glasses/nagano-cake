<div class='container'>
  <div class='row'>
    <div class='col-md-2'>
      <%= render 'public/items/category_search', category: @categories %>
    </div>
    <div class='col-md-10'>
      <div>
        <p class="border">
          <br />
          ようこそ、ながのCAKEへ！<br />
          このサイトは、ケーキ販売のECサイトになります。<br />
          会員でない方も商品の閲覧はできますが、<br />
          購入には会員登録が必要になります。<br />
          <br />
        </p>
      </div>
      <h4>新着商品</h4>
      <% @item.each do |item| %>
        <div class="item">
          <%= attachment_image_tag item, :image, size: "200x150" %><br />
          <%= link_to item.name, item_path(item), class: "item_#{item.id}" %><br />
          <!--item.rbでの計算結果を3桁ごとにカンマ区切りで表示-->
          ￥<%= item.add_tax_price.to_s(:delimited) %> （税込）
        </div>
      <% end %>
      <div>
        <%=link_to "全ての商品を見る >", items_path %>
      </div>
    </div>
  </div>
</div>